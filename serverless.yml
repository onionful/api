service: onionful

plugins:
  - serverless-webpack
  - serverless-offline
  - serverless-domain-manager
  - serverless-aws-alias

provider:
  name: aws
  runtime: nodejs8.10
  region: eu-west-1
  stage: ${env:ENVIRONMENT, 'development'}
  environment:
    STAGE: ${self:provider.stage}
  iamRoleStatements:
    $ref: ./roleStatements.yml
  apiName: ${self:service}
  stackName: ${self:service}

custom:
  serverless-offline:
    host: ${env:OFFLINE_HOST}
    port: ${env:OFFLINE_PORT}
  customDomain:
    domainName: ${env:DOMAIN}
    stage: ${self:provider.stage}
    createRoute53Record: true
    endpointType: regional

functions:
  # auth

  auth:
    handler: src/functions/auth.check
    name: ${self:service}-auth
    cors: true

  rotateToken:
    handler: src/functions/auth.rotateToken
    name: ${self:service}-rotateToken
    cors: true
    events:
      - schedule: rate(8 hours)
      # TODO remove on production
      - http:
          path: rotate
          method: get
          cors: true

  # users

  usersList:
    handler: src/functions/users.list
    name: ${self:service}-usersList
    events:
      - http:
          path: users
          method: get
          authorizer: auth
          cors: true

  # profile

  profileGet:
    handler: src/functions/profile.get
    name: ${self:service}-profileGet
    events:
      - http:
          path: profile
          method: get
          authorizer: auth
          cors: true

  profileUpdate:
    handler: src/functions/profile.update
    name: ${self:service}-profileUpdate
    events:
      - http:
          path: profile
          method: put
          authorizer: auth
          cors: true

  # collections

  collectionsList:
    handler: src/functions/collections.list
    name: ${self:service}-collectionsList
    events:
    - http:
        path: collections
        method: get
        cors: true

  collectionsGet:
    handler: src/functions/collections.get
    name: ${self:service}-collectionsGet
    events:
    - http:
        path: collections/{id}
        method: get
        cors: true

  collectionsCreate:
    handler: src/functions/collections.create
    name: ${self:service}-collectionsCreate
    events:
      - http:
          path: collections
          method: post
          authorizer: auth
          cors: true

  collectionsUpdate:
    handler: src/functions/collections.update
    name: ${self:service}-collectionsUpdate
    events:
      - http:
          path: collections/{id}
          method: put
          authorizer: auth
          cors: true

  collectionsDelete:
    handler: src/functions/collections.remove
    name: ${self:service}-collectionsDelete
    events:
      - http:
          path: collections/{id}
          method: delete
          cors: true

  # contents

  contentCreate:
    handler: src/functions/contents.create
    name: ${self:service}-contentCreate
    events:
      - http:
          path: contents
          method: post
          authorizer: auth
          cors: true

  contentUpdate:
    handler: src/functions/contents.update
    name: ${self:service}-contentUpdate
    events:
      - http:
          path: contents/{id}
          method: put
          authorizer: auth
          cors: true

  contentGet:
    handler: src/functions/contents.get
    name: ${self:service}-contentGet
    events:
      - http:
          path: contents/{id}
          method: get
          cors: true

  contentList:
    handler: src/functions/contents.list
    name: ${self:service}-contentList
    events:
      - http:
          path: contents
          method: get
          cors: true

  # spaces

  spacesCreate:
    handler: src/functions/spaces.create
    name: ${self:service}-spacesCreate
    events:
      - http:
          path: spaces
          method: post
          authorizer: auth
          cors: true

  spacesUpdate:
    handler: src/functions/spaces.update
    name: ${self:service}-spacesUpdate
    events:
      - http:
          path: spaces/{id}
          method: put
          authorizer: auth
          cors: true

  spacesGet:
    handler: src/functions/spaces.get
    name: ${self:service}-spacesGet
    events:
      - http:
          path: spaces/{id}
          method: get
          cors: true

  spacesList:
    handler: src/functions/spaces.list
    name: ${self:service}-spacesList
    events:
      - http:
          path: spaces
          method: get
          cors: true

create:
  handler: handler.create
  events:
  - http:
      path: /
      method: post
      authorizer: auth-check
      cors: true

update:
  handler: handler.update
  events:
  - http:
      path: /{id}
      method: put
      authorizer: auth-check
      cors: true

get:
  handler: handler.get
  events:
  - http:
      path: /{id}
      method: get
      cors: true

list:
  handler: handler.list
  events:
  - http:
      path: /
      method: get
      cors: true

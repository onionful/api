get:
  handler: handler.get
  events:
  - http:
      path: profile
      method: get
      authorizer: auth-check
      cors: true

update:
  handler: handler.update
  events:
  - http:
      path: profile
      method: put
      authorizer: auth-check
      cors: true
